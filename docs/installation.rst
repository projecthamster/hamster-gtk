============
Installation
============

At the command line::

    $ easy_install hamster-gtk

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv hamster-gtk
    $ pip install hamster-gtk
